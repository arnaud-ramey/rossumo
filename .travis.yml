# travis rules for rossumo
language: cpp
compiler:
  - gcc

before_install:
  # install required packages
  - sudo apt-get install phablet-tools  autoconf  libavahi-client-dev  libavcodec-dev  libavformat-dev  libswscale-dev
  # install ROS
  - export ROS_DISTRO=hydro
  - sudo sh -c 'echo "deb http://packages.ros.org/ros/ubuntu precise main" > /etc/apt/sources.list.d/ros-latest.list'
  - wget https://raw.githubusercontent.com/ros/rosdistro/master/ros.key -O - | sudo apt-key add -
  - sudo apt-get -qq update
  - sudo apt-get install -y -qq ros-$ROS_DISTRO-ros-base  ros-$ROS_DISTRO-nav-msgs  ros-$ROS_DISTRO-std-msgs    ros-$ROS_DISTRO-tf  ros-$ROS_DISTRO-std-msgs    ros-$ROS_DISTRO-tf2
  - source /opt/ros/$ROS_DISTRO/setup.bash

before_script: # Use this to prepare your build for testing e.g. copy database configurations, environment variables, etc.
  # Create catkin workspace
  - export CI_SOURCE_PATH=$(pwd)
  - mkdir -p ~/catkin_ws/src
  - cd ~/catkin_ws/src
  - catkin_init_workspace
  - ln -s $CI_SOURCE_PATH . # Link the repo we are testing to the new workspace

script: # compile
  - cd ~/catkin_ws
  - catkin_make
